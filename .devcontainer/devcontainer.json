{
    "name": "DevOps Development Container (SSH + Dotfiles)",
    "image": "yourusername/devops-container:latest",
    "forwardPorts": [22, 3000, 8000, 8080, 9090],
    "portsAttributes": {
        "22": {
            "label": "SSH",
            "requireLocalPort": true
        },
        "3000": {
            "label": "Development Server"
        },
        "8000": {
            "label": "HTTP Server"
        },
        "8080": {
            "label": "Application Server"
        },
        "9090": {
            "label": "Monitoring"
        }
    },
    "postCreateCommand": [
        "bash", "-c",
        "echo '🚀 Setting up DevOps container with dotfiles...' && echo '📁 Cloning dotfiles repository...' && git clone https://github.com/ReCoL1337/.dotfiles.git ~/.dotfiles && cd ~/.dotfiles && echo '⚙️ Setting up dotfiles...' && if [ -f install.sh ]; then bash install.sh; elif [ -f setup.sh ]; then bash setup.sh; elif [ -f bootstrap.sh ]; then bash bootstrap.sh; elif [ -f Makefile ]; then make install || make; elif [ -f install ]; then bash install; else echo '⚠️ No install script found, you may need to manually setup dotfiles'; fi && echo '✅ Dotfiles setup complete!' && echo '🔐 Setting up SSH daemon...' && sudo service ssh start && echo '🔧 Verifying tools...' && go version && cargo --version && terraform --version && helm version --client && nvim --version && echo '🎉 Container ready! SSH: ssh dev@localhost -p 22'"
    ],
    "remoteUser": "dev",
    "runArgs": [
        "--init",
        "--cap-add=SYS_PTRACE",
        "--security-opt", "seccomp=unconfined"
    ],
    "shutdownAction": "stopContainer",
    "containerEnv": {
        "SHELL": "/bin/zsh",
        "TERM": "xterm-256color",
        "EDITOR": "nvim",
        "VISUAL": "nvim"
    },
    "workspaceMount": "source=${localWorkspaceFolder},target=/home/dev/workspace,type=bind,consistency=cached",
    "workspaceFolder": "/home/dev/workspace"
}